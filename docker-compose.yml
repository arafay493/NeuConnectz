version: '3.9'

services:
  z-connect:
    build: .
    container_name: z-connect-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
    restart: unless-stopped
